"use client";

import { AnimatePresence, motion } from "motion/react";
import { useState } from "react";
import { Reveal } from "../motion/Reveal";

export function AICopilotDemo() { const [executed,setExecuted]=useState(false); return <section className="agent-section"><div className="section-shell agent-grid"><Reveal><p className="marketing-eyebrow light">ACTNIVO AI</p><h2 className="section-title light">Ask your business<br/>anything.</h2><p>From marketplace failures to the next best action, Actnivo explains what matters in language your team can act on.</p></Reveal><Reveal variant="slideLeft" className="chat-window"><div className="chat-head"><span>✦</span><div><b>Actnivo AI</b><small>Watching 9 channels</small></div><i>Online</i></div><div className="user-message">What’s going wrong today?</div><div className="ai-message"><span>A</span><div><p>I found 8 issues representing approximately <b>₹84,300</b> in revenue at risk.</p><div className="mini-actions"><i>Blinkit stockout · ₹34K</i><i>Zepto PO shortage · ₹27K</i><i>6 more issues · ₹23.3K</i></div></div></div><div className="user-message">Fix everything safe.</div><div className="ai-message"><span>A</span><div><p>5 actions are within your automation policy.</p><div className="chat-actions"><button>Review actions</button><button className="execute" onClick={()=>setExecuted(true)} disabled={executed}>{executed?"✓ 5 executed":"Execute 5"}</button></div><AnimatePresence>{executed&&<motion.small initial={{opacity:0,y:6}} animate={{opacity:1,y:0}}>Actions are running. I’ll verify each outcome.</motion.small>}</AnimatePresence></div></div></Reveal></div></section>; }
